# src/components/settings/AGENTS.md

Settings panels for providers and app configuration.

## Conventions

- Never log or display secrets
- Persist via storage/services layer, not browser APIs directly
- Keep provider-specific logic in hooks/services
